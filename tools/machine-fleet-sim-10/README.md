# Local 10-device machine fleet simulator

**Not part of the SDK.** For dev/test only.

Single ESP32 on WiFi `71` publishes to MQTT `192.168.68.107:1883` for all 10 device tokens.

See **SETTINGS.md** for widget threshold table and per-device behaviour.
